# Swift / SwiftPM
cli/.build/
.build/
*.dSYM/

# Node
node_modules/
mcp/dist/

# App build outputs (regenerate with apps/AgentTasks/build.sh)
apps/AgentTasks/build/
research/ClaudeLanguageModel/build/

# macOS
.DS_Store

# Local-only config lives in ~/.config/apple-tasks (agents.json, notify.json,
# apple-tasks.db, runs/) — never in the repo.
