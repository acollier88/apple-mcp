# Research / spikes

Work that is **not** part of either product yet. Promote into `cli/` or
`apps/AgentTasks/` when ready; don’t wire production paths here.

| Spike | Notes |
|-------|--------|
| `ClaudeLanguageModel/` | In-process Claude via FoundationModels (`LanguageModel` protocol). Blocked on SDK/OS skew — see [`../docs/claude-language-model-spike.md`](../docs/claude-language-model-spike.md). |
